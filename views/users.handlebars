<main>
    <h1>Users</h1>
    {{#if hasUsers}}
    <ul>
        {{#each users}}
            <li>{{ this.username }}</li>
        {{/each}}
    </ul>
    {{else}}
    <h1>No Users Found!</h1>
    {{/if}}
</main>